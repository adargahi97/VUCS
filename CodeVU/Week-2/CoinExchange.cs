using System;

namespace CodeVU
{/*
    internal class CoinExchange
    {
        static void Coins(string[] args)
        {
            int quarters = 0;
            int dimes = 0;
            int nickels = 0;
            int pennies = 0;


            string amount = Console.ReadLine();
            var price = double.Parse(amount);
            Console.WriteLine(price);


            tempNum = price / 0.25;
            int quarters = (int)tempNum;
            price = price - (0.25 * quarters);
            Console.WriteLine(price);

            tempNum = price / 0.10;
            int dimes = (int)tempNum;
            price = price - (0.25 * dimes);
            Console.WriteLine(price);

            tempNum = price / 0.05;
            int nickels = (int)tempNum;
            price = price - (0.25 * nickels);
            Console.WriteLine(price);

            tempNum = price / 0.01;
            int pennies = (int)tempNum;

            string final = String.Format("{0} quarters, {1} dimes, {2} nickels, {3} pennies", quarters, nickels, dimes, pennies);

            Console.WriteLine(final);
        }
    }*/
}
